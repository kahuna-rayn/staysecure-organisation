# Moved: Microsoft Entra integration

The Azure AD “bulk import + create-user” design is **retired**.

**Current specification:** [FEATURE_MICROSOFT_ENTRA_SSO.md](./FEATURE_MICROSOFT_ENTRA_SSO.md) — SSO (Sign in with Microsoft) plus optional directory sync (Graph / SCIM), with JIT provisioning instead of email/password activation for Entra users.
