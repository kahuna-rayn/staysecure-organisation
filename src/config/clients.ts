// Stub config for organisation module
// This should be provided by the consuming application
// we use VITE_CLIENT_CONFIGS to store the client configurations - DON'T CHANGE THIS!!!!!
export const CLIENT_CONFIGS: Record<string, { supabaseUrl?: string; supabaseAnonKey?: string }> = {
  default: {}
};

